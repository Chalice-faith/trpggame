# TRPG Game - Go Backend Configuration
# 开发环境配置

server:
  port: "8080"
  mode: debug

database:
  host: localhost
  port: "3306"
  user: root
  password: 123456
  dbname: trpggame
  charset: utf8mb4
  loc: Local

redis:
  addr: localhost:6379
  password: ""
  db: 0

jwt:
  secret: "dev-secret-change-in-production-please"
  accesstokenttl: 15
  refreshtokenttl: 168

ai:
  baseurl: http://localhost:8000
  timeout: 60

minio:
  endpoint: localhost:9000
  accesskey: admin
  secretkey: adminadmin
  bucket: trpg-scripts
  usessl: false
  maxuploadsize: 52428800 # 50 MiB，单位：字节

internal:
  shared_secret: "dev-internal-secret-change-in-production"
