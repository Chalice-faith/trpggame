# TRPGGame M2.2 验收记录

> **记录日期**：2026-09-15
>
> **当前结论**：M2.2-A—C 已完成；M2.2-D 代码级实现与本地自动化验收通过，等待 GitHub Actions 和真实双账号浏览器验收。M2.2 尚不能标记为完整验收通过。

## 1. 本次代码级验收范围

- 私聊会话创建/复用、列表、历史分页与已读水位 REST。
- `chat_message`/`chat_ack`、`conversation_updated`、`im_sync`/`im_sync_batch` 可靠投递。
- Vue `/chat/:conversationId?`、聊天 API/Store、好友与控制台入口。
- 乐观发送、ack 权威替换、失败重试、seq 去重、缺口同步、未读、断线禁用、登出清理和删好友后历史只读。
- DEV-007 的 SQL/MySQL 同步查询回归与 DEV-008 的 REST/WS 错误映射统一。

## 2. 自动化验证结果

| 检查 | 命令或环境 | 结果 |
|---|---|---|
| Go 全量测试 | `go test ./...` | 通过 |
| Go 静态检查 | `go vet ./...` | 通过 |
| Go 服务构建 | `go build ./cmd/server` | 通过 |
| Linux targeted race | WSL Docker `golang:1.22-bookworm`，只读源码与本机只读模块缓存；repo/service/realtime/ws/imws/router 六包 | 通过 |
| MySQL 8.4 集成 | 临时 `mysql:8.4` 容器，空库执行 001—013 迁移后运行 `TestChatServiceMySQL84Concurrency` | 通过；容器已移除 |
| Vue 测试 | `npm test` | 7 个测试文件、28 项测试全部通过 |
| Vue 类型与生产构建 | `npm run build` | 通过 |
| Python 编译与测试 | 项目 `.venv` 执行 `compileall` 与 `unittest` | 105 项测试全部通过 |
| OpenAPI | `npm run api:lint` | 通过 |
| 数据库迁移测试 | `npm run db:test` | 12 项测试全部通过 |
| Compose 配置 | WSL Docker `docker compose config --quiet` | 通过 |

## 3. 前端回归覆盖

- Chat Store：pending→ack、失败后复用同一 UUID 重试、按 seq 去重、跳号触发同步、断线保留失败消息及登出清理。
- Chat 页面：打开会话与历史、发送和 ack、旧消息分页、断线禁用、失败重试、删好友后只读。
- IM Store：聊天 ack 与入站消息分发；原有自动连接、退避重连、Token 刷新、4001 接管、presence 与好友关系事件继续通过。
- 好友页：创建或复用私聊并跳转；原有搜索、申请、处理申请、删除好友和 presence 回归继续通过。

## 4. 非阻塞告警

- Vue 生产构建继续报告主包超过 500 KiB（DEV-002）与 VueUse PURE 注解位置警告（DEV-003），均为存量问题，不影响构建成功。
- Python 测试继续偶发 AnyIO 内存流 `ResourceWarning`（DEV-004），105 项测试仍全部通过。
- Compose 解析提示顶层 `version` 字段已废弃但会被忽略，不影响配置有效性。
- 首次 Linux race 容器从 `proxy.golang.org` 拉取依赖超时；改用本机只读 Go 模块缓存后六包全部通过，属于验证环境网络问题。

## 5. 尚未执行的真实验收

- 提交当前改动并确认 GitHub Actions 所有作业通过。
- 使用两个真实账号登录浏览器，从好友页创建或复用私聊。
- 验证发送端 pending→ack、接收端实时消息、双方会话排序与未读数。
- 断开接收端后连续发送多条消息，重连验证 `im_sync` 分批补齐且无重复遗漏。
- 刷新聊天页面，验证 REST 历史恢复与本地 seq 水位衔接。
- 删除好友后验证历史可读、发送禁用；重新添加后复用原会话并恢复发送。
- 验证同账号第二页面连接接管、旧页面 4001 关闭且不会争用聊天状态。

## 6. 验收结论

M2.2-D 的开发和本地代码级验收已经完成，DEV-007/008 已解决。由于当前改动尚未经过 GitHub Actions，且真实双账号浏览器、断线和刷新恢复尚未执行，本记录保持“代码级通过、完整验收待完成”。上述外部验收关闭后，方可把 M2.2 标记为完成并评审是否进入 M2.3。
