# 开发交接与恢复说明

> **更新日期**：2026-09-09
>
> **当前分支**：`dev/phase2-planning`
>
> **本次 D2 实施前代码基线**：`7198ff8 fix: 完善游戏 WebSocket 连接接管与生命周期`
>
> **下一开发入口**：规划并实施 M2.0-D / D3（双通道隔离、握手矩阵和 Token 日志安全回归）。

## 1. 当前里程碑状态

### Phase 1

- M1.1—M1.5 代码闭环已完成：用户、剧本、Python AI、单人游戏、存读档、WebSocket 流式行动和 Vue 单人游戏界面均已实现。
- 公共 REST OpenAPI、Swagger UI、Gin 路由契约测试和 Redocly CI 校验已完成。
- 数据库迁移已从 Go 启动流程拆出，由根目录 npm 迁移工具和 Docker `migrate` 服务负责。
- Phase 1 真实部署与端到端接口验收仍未执行，P1-02—P1-05 保持未关闭；2026-09-08 经开发者决定暂缓，先推进 Phase 2 基础设计。

### Phase 2 / M2.0

- M2.0 总体方案与产品决策已确认；本里程碑只建设契约和实时通信基础，不实现好友、聊天、群组或多人房间业务。
- M2.0-A 已完成：冻结 Phase 2 数据库、REST、错误码和 WebSocket 契约；新增 Origin 配置、公共查询参数 JWT 鉴权和 IM 消息信封。
- M2.0-B 已完成：游戏 WebSocket 迁移到 `internal/realtime` 公共 Origin/JWT/Upgrader 组件，并保持 Phase 1 路径与错误码兼容。
- M2.0-C 已完成：新增 `/ws/im?token=<JWT>`、IM Hub/Client、严格消息解析、Ping/Pong、同用户最新连接接管和双通道独立运行。
- M2.0-D 实施中：D1 已完成游戏通道连接生命周期修复；D2 已完成 IM 并发、接管风暴、Stop 竞争、心跳、异常断开与慢连接验证，未发现需修改 IM Hub 生产逻辑的问题；D3—D4 尚未实施。

## 2. 关键提交记录

| 提交 | 内容 |
|---|---|
| `f335c81` | OpenAPI 路由契约与规范校验 |
| `c84ebb7` | M2.0 总体实施方案 |
| `edd4eb0` | M2.0-A 实时通信配置与协议基础 |
| `e0c21fc` | M2.0-B WebSocket 公共握手组件 |
| `f74d02c` | M2.0-C IM WebSocket 连接基础 |
| `35a62af` | M2.0-D 并发、异常关闭与收尾设计 |

## 3. 当前实时通信结构

| 通道 | 地址 | 作用 | 当前状态 |
|---|---|---|---|
| 游戏 | `/ws?token=<JWT>&room_id=<id>` | 房间订阅、行动流式叙事、状态事件与断线补推 | D1 已实现同房间同用户最新连接接管 |
| IM | `/ws/im?token=<JWT>` | 用户级社交通道 | D2 并发、心跳、异常关闭与慢连接自动化验证已完成 |

- 两个通道共享 `internal/realtime` 的查询参数 JWT 鉴权、Origin 白名单和 Upgrader 配置，但各自维护连接表，互不接管。
- IM 同用户新连接会收到新的 `connection_id`，旧连接收到 `connection_replaced` 后以应用关闭码 `4001` 关闭。
- IM 业务类型尚未实现；除 Ping 外的入站消息返回 `1705 unsupported message type`。
- Gin 默认访问日志跳过 `/ws` 与 `/ws/im`，避免记录含 Token 的完整查询串；其他代理层仍需按部署指南关闭敏感查询日志。

## 4. 一致性与并发边界

- MySQL 是房间生命周期、玩家关系和存档的持久化权威；Redis 是实时运行态权威。
- Redis 行动提交通过请求 UUID、请求指纹、预期回合和运行态世代完成原子 CAS。
- 暂停从 `playing` 进入 `paused` 时原子轮换世代；恢复不会使旧世代重新有效。
- 读档先进入暂停隔离态，再原子替换 Redis 快照、清理旧行动缓存和自动存档待办，最后精确同步 MySQL 回合字段。
- 普通行动使用 MySQL `GREATEST` 单调推进进度，迟到请求不能覆盖更大的回合号。
- 自动存档先在 Redis 保存提交时的精确快照，再写入 MySQL；失败时同一请求可重试，且不会重复调用 AI。
- 结束游戏采用 Redis 与 MySQL 状态切换及补偿确认，并清理运行态。
- `trigger_event` 当前只随行动结果返回，尚未写入计划中的关键事件表。

## 5. 验证边界

- Phase 1 自动化检查与 GitHub Actions 已通过；真实 MySQL、Redis、Milvus、MinIO、DeepSeek、浏览器和 Docker Compose 端到端验收仍未完成。
- M2.0-A/B/C 已包含配置、Origin、JWT、协议、IM Hub/Client、真实 WebSocket 升级和路由测试。
- M2.0-D / D1、D2 已通过专项高重复、受影响包 20 次重复、Go 全量 test/vet/build；尚不能声称双通道安全回归或 targeted race 已通过。
- Windows 本机如仍存在 `GOROOT` 误设，Go 命令需使用正确的 `G:/Go/go`；race 结果以支持 CGO 的 Linux/CI 为准。

以下真实外部依赖验证仍未执行，不得误写为已通过：

- 在真实 MySQL 实例从空库执行全部迁移及事务/锁验证；
- 真实 Redis 服务上的 Lua 脚本联调；
- Go → Python AI 行动端到端调用；
- 真实 DeepSeek、Milvus、MinIO 联调；
- Docker Compose 全链路部署验收；
- WebSocket 流式推送及 Vue 游戏页面验收。

## 6. 下一阶段开发顺序

1. D1 已完成并提交：游戏 Client/Hub 已增加连接 ID、最新连接接管、旧连接防误删和幂等 Stop。
2. D2 已完成并获开发者评审通过：IM 并发、心跳、异常关闭、慢连接与停止竞争测试均通过。
3. D3：补充同用户游戏与 IM 双通道并存、握手矩阵和 Token 日志隔离回归。
4. D4：接入 targeted race CI，执行完整自动化检查并新建 M2.0 验收记录。
5. M2.0-D 评审和提交完成后停止；是否进入 M2.1 好友与在线状态由开发者另行确认。

## 7. 恢复开发检查清单

1. 确认分支为 `dev/phase2-planning`，核对 `git status --short --branch` 和最近提交。
2. 以 `docs/未完成事项.md`、`docs/M2.0实施方案.md` 和 `docs/M2.0-D设计.md` 为当前任务依据。
3. 不丢弃或覆盖来源不明的工作区修改。
4. D1、D2 已提交或获准提交；下一步先完成 D3 规划，不提前进入 M2.1。
5. 实施 D 阶段时按 D1—D4 分块验证；每块完成后先汇报变更、风险和验证结果。
6. 不把 Phase 1 真实部署验收、M2.0-D 或 Phase 2 业务功能提前标记为完成。
