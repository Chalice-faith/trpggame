# 开发交接与恢复说明

> **更新日期**：2026-08-14
>
> **当前分支**：`dev/feature`
>
> **本次文档整理前代码基线**：`104ceda feat(ws): 完成 WebSocket 事件信封与鉴权订阅基础层`
>
> **下一开发入口**：Phase 1 / M1.5 行动流式事件链路（Python 流式端点 → Go 流式客户端 → WebSocket 事件推送）。

## 1. 当前里程碑状态

- M1.1 项目骨架与基础设施已完成。
- M1.2 用户系统已完成。
- M1.3 剧本上传、解析、向量化和前端管理功能已完成；真实 Docker Compose 端到端验收仍暂缓。
- M1.4 Python AI 同步推理核心已完成；真正的流式输出仍属于后续 WebSocket 链路。
- M1.5 单人游戏后端闭环已完成：
  - MySQL 房间、玩家、存档模型及 `001`—`008` 迁移；
  - 通用迁移执行器、全局 advisory lock、迁移版本及校验值记录；
  - Redis 玩家状态、道具、Buff、摘要、最近消息和行动幂等缓存；
  - 单人快速开始和同步行动 REST 接口；
  - 手动存档、存档列表、读档、暂停、恢复和结束 REST 接口；
  - 每 10 回合自动存档、Redis 待持久化快照及 MySQL 幂等唯一约束；
  - MySQL 回合字段同步与读档时的精确进度替换；
  - 运行态世代隔离，防止暂停或读档前的旧 AI 结果污染新时间线。
- M1.5 WebSocket 基础层已完成：
  - 事件信封：`request_id` 关联、按房间单调 `seq`、`subscribed` 订阅确认、`sync`/`sync_batch` 断线补推、`error` 错误事件；
  - 鉴权接入：WS 升级改为 `?token=<JWT>&room_id=<id>`，JWT 校验 + `RoomAuthorizer` 房间订阅校验；
  - Hub/Client 重写：按房间 seq 单 goroutine 分配、recent 环形缓冲补推、`HandleInbound` 客户端消息分发、register/unregister 竞态加固。
- M1.5 尚未完成：行动流式事件链路（行动仍走同步 REST）、Vue 单人游戏页面。

## 2. 关键提交记录

| 提交 | 内容 |
|---|---|
| `104ceda` | WebSocket 事件信封、JWT 鉴权订阅、Hub/Client 重写与断线补推 |
| `db19b88` | 单人游戏 MySQL 模型、迁移与 CRUD |
| `eccc3f8` | Redis 运行态、幂等提交及快照仓储 |
| `5c25720` | 快速开始与玩家行动同步 REST 链路 |
| `415c09c` | 手动存档 Service |
| `d8ed520` | 通用 MySQL 迁移执行器及自动存档唯一约束 |
| `2b33e7b` | 存读档、自动存档和房间生命周期后端闭环 |

## 3. 已开放的单人游戏接口

所有接口均位于 `/api/v1`，并要求 JWT 鉴权。

| 方法 | 路径 | 状态 |
|---|---|---|
| `POST` | `/games/solo/start` | 已完成 |
| `POST` | `/games/:roomId/action` | 已完成，同步 REST 响应 |
| `POST` | `/games/:roomId/save` | 已完成 |
| `GET` | `/games/:roomId/saves` | 已完成 |
| `POST` | `/games/:roomId/load` | 已完成，读档后保持暂停 |
| `POST` | `/games/:roomId/pause` | 已完成 |
| `POST` | `/games/:roomId/resume` | 已完成 |
| `POST` | `/games/:roomId/end` | 已完成 |

当前行动接口不是 SSE 或 WebSocket 流式输出。不得把同步 REST 叙事响应描述为已完成流式推送。

## 4. 一致性与并发边界

- MySQL 是房间生命周期、玩家关系和存档的持久化权威；Redis 是实时运行态权威。
- Redis 行动提交通过请求 UUID、请求指纹、预期回合和运行态世代完成原子 CAS。
- 暂停从 `playing` 进入 `paused` 时原子轮换世代；恢复不会使旧世代重新有效。
- 读档先进入暂停隔离态，再原子替换 Redis 快照、清理旧行动缓存和自动存档待办，最后精确同步 MySQL 回合字段。
- 普通行动使用 MySQL `GREATEST` 单调推进进度，迟到请求不能覆盖更大的回合号。
- 自动存档先在 Redis 保存提交时的精确快照，再写入 MySQL；失败时同一请求可重试，且不会重复调用 AI。
- 结束游戏采用 Redis 与 MySQL 状态切换及补偿确认，并清理运行态。
- `trigger_event` 当前只随行动结果返回，尚未写入计划中的关键事件表。

## 5. 已完成验证

2026-08-10 验证结果：

- `go test ./...`：通过；
- `go vet ./...`：通过；
- `go build ./...`：通过；
- `git diff --check`：通过；
- 关键存档、生命周期、迁移和并发专项测试重复 10 次：通过；
- `go test -race ./...`：通过，未发现数据竞争。

2026-08-14 新增验证（WebSocket 基础层）：

- 新增 ws 包 15 个单元/握手测试：seq 单调、定向投递、`sync` 断线补推、未知消息回 `error`、JWT 各失败分支、真实 WS 升级握手、register/unregister 竞态；
- `go test ./...`、`go vet ./...`、`go build ./...`、`git diff --check`：通过；
- Windows 本机无 CGO，`go test -race` 无法运行（沿用 WSL 容器方案）；本机 `GOROOT` 环境变量被错设为 `G:\Go`，正确为 `G:\Go\go`，所有 go 命令需加前缀 `GOROOT=G:/Go/go`。

Windows Go 环境未启用 CGO，因此 race 检测使用 WSL 中的一次性 `golang:1.22-bookworm` 容器执行；工作区和本机 Go 模块缓存均只读挂载，并使用 `--network none`，没有启动项目服务或连接数据库。

以下真实外部依赖验证仍未执行，不得误写为已通过：

- 在真实 MySQL 实例从空库执行全部迁移及事务/锁验证；
- 真实 Redis 服务上的 Lua 脚本联调；
- Go → Python AI 行动端到端调用；
- 真实 DeepSeek、Milvus、MinIO 联调；
- Docker Compose 全链路部署验收；
- WebSocket 流式推送及 Vue 游戏页面验收。

## 6. 下一阶段开发顺序

已完成：WebSocket 事件信封（序列号、错误、重连补推语义）、Hub/Client 接入 JWT 鉴权与房间订阅。

1. 实现行动流式事件链路：
   a. Python 新增流式推理端点（复用 `ActionInferenceService`，最终叙事改走 `chat_stream`，NDJSON 输出 chunks + 结尾元数据）；
   b. Go `ai_client` 新增流式客户端（解析 NDJSON，支持上下文取消）；
   c. Go `GameService` 新增流式行动方法（复用校验/幂等/提交逻辑，chunks 实时推 WS），`game_handler` 接入 `game_action` 事件；
   d. 事件序列：`narrative_chunk`×N → `dice_roll`/`status_update` → `narrative_complete`，失败发 `error`（含 request_id）；同步 REST 过渡期保持兼容。
2. 实现 Vue `websocket.ts` store（JWT 连接、心跳、重连、seq 去重）与 `game.ts` 流式状态。
3. 开发单人选角和游戏页面，再进行前后端联调。
4. 条件允许时，单独补做真实 MySQL、Redis、Python 和 Docker 验收。

## 7. 恢复开发检查清单

1. 确认分支为 `dev/feature`，核对 `git status --short` 和最近提交。
2. 不丢弃或覆盖工作区中来源不明的用户修改（如未提交的 README.md 文档改动）。
3. 本机所有 go 命令加前缀 `GOROOT=G:/Go/go`（本机 GOROOT 环境变量被错设为 `G:\Go`）。
4. 运行 `go test ./...`、`go vet ./...`、`go build ./...` 和 `git diff --check`。
5. 每个代码块完成后先交用户审核，再进入下一块。
6. 不提前把尚未实现的流式链路、Vue 页面或真实部署验收标记为完成。
