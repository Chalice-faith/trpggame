# M1.3 剧本系统验收记录

> **记录日期**：2026-07-29  
> **当前结论**：项目已暂停开发；代码级验收通过，Docker Compose 真实端到端验收待执行。

> **恢复说明**：恢复开发前先按 [开发暂停交接.md](./开发暂停交接.md) 核对分支、未提交变更和验收顺序。

## 1. 已完成范围

- Go：PDF 上传校验、MinIO 存储、分页列表、详情、删除和用户归属校验。
- Go：Python 解析任务调用、内部状态回写、共享密钥鉴权及 `chunk_count` 持久化。
- Go：失败剧本重新解析接口、用户归属校验及原子状态切换。
- Python：MinIO 临时下载、逐页 PDF 提取、文本清洗、章节切片和页码元数据。
- Python：BGE 1024 维向量生成、Milvus Collection、批量写入和重复解析幂等清理。
- Python：后台解析编排、成功/失败回调和临时文件异常路径清理。
- 删除闭环：Milvus 向量、MinIO 对象和 MySQL 记录依次清理。
- Vue：剧本列表、分页、状态轮询、PDF 上传、详情、错误展示和删除确认。
- Vue：失败剧本重新解析入口及重复提交保护。
- Vue：Vitest 自动化覆盖 PDF 上传校验、解析状态轮询终止、错误及重试入口展示。

## 2. 自动化检查结果

| 检查项 | 结果 | 说明 |
|---|---|---|
| `go test ./...` | 通过 | Repository/Service/Handler/AI Client 相关测试通过 |
| `go vet ./...` | 通过 | 未发现 Go 静态检查错误 |
| Python `compileall` | 通过 | `app` 与 `tests` 可编译 |
| Python `unittest` | 通过 | 41 个测试通过 |
| `npm run build` | 通过 | TypeScript 检查和 Vite 生产构建成功 |
| `npm test` | 通过 | 2 个测试文件、7 项前端测试通过 |
| `git diff --check` | 通过 | 未发现空白符错误 |
| `docker compose config --quiet` | 待执行 | 当前开发终端没有 Docker CLI |

统一执行入口：

```powershell
.\scripts\verify-m13.ps1
```

正式验收要求 Docker 检查不可跳过：

```powershell
.\scripts\verify-m13.ps1 -RequireDocker
```

## 3. 尚待真实环境验证

- 在全新 MySQL 实例依次执行 `001` 至 `004` 迁移。
- 启动 MySQL、MinIO、etcd、Milvus、Go、Python 和 Vue 全套 Compose 服务。
- 上传正常文本 PDF，确认状态最终变为 `ready`，且 Milvus 元数据和 `chunk_count` 正确。
- 上传带重复页眉页脚 PDF，抽查清洗和页码映射。
- 上传扫描型或空白 PDF，确认状态最终变为 `failed` 且错误可读。
- 删除已完成剧本，确认 MySQL、MinIO 和 Milvus 均无残留。
- 对同一剧本执行重复解析，确认 Milvus 不产生重复向量。

## 4. 已知非阻塞事项

- 后台任务仍为进程内任务；服务重启可能遗留长时间 `parsing` 状态。
- OCR、WebSocket 解析进度和自动提取预设角色不属于 M1.3 范围。
- 前端构建仍有主包体积及第三方 PURE 注解警告，详见
  [开发问题记录.md](./开发问题记录.md)。
