# M2.5-F 真实验收记录

日期：2026-09-23  
分支：`dev/phase2-planning`  
范围：多人房间生命周期、V2 运行态、存档、流式行动、断线恢复和结束后只读回看。

## 环境边界

- 使用隔离的 MySQL 8.4、Redis、MinIO 容器，端口映射到本机临时端口；测试数据和账号均为合成数据。
- 使用三个独立浏览器客户端和三个测试账号验证跨客户端同步。另建第二个房间验证超时推进与权限拒绝。
- AI 使用仓库内 `scripts/acceptance/m25f-ai.mjs` 的确定性 NDJSON fixture，包含正常流式、慢生成和固定完成结果；本次不代表生产 DeepSeek、Milvus 或 Nginx 的连通性/质量验收。

## 场景与结果

| 场景 | 结果 |
|---|---|
| 三账号创建/加入、选角、准备、房主开局 | 通过；三端看到同一开场、队列、当前行动者和倒计时 |
| 当前行动者流式提交、其他成员跟随 | 通过；临时 chunk 只在草稿区显示，完成后才进入正式叙事，三端 seq 一致 |
| 非当前行动者操作 | 通过；UI 控件禁用，伪造 REST 请求返回 HTTP 403、错误码 1922 |
| 主动跳过与服务端超时自动跳过 | 通过；第二房间把超时压到 30 秒，等待后所有客户端水位和当前行动者同步推进 |
| 暂停、恢复、手动存档、读档 | 通过；读档回到保存的第二轮并保持 paused，恢复后重新创建 deadline |
| 五个完整轮次自动存档 | 通过；出现 `自动存档-5轮`，存档列表回合显示与后端水位一致 |
| 慢生成期间暂停/取消 | 通过；能看到部分临时 chunk，暂停后旧 generation 不再提交第二条正式叙事 |
| 浏览器刷新、断线重连 | 通过；按 REST 权威快照恢复，成员和角色未被移除 |
| 结束游戏及刷新后的只读回看 | 通过；结束态可读取最近记录和存档，行动/房主控制均禁用 |

## 验收期间修复

- 忽略旧版本实时快照，避免重连时误撤销仍在房间内的成员。
- 结束态运行快照允许在保留期内读取，刷新后仍能展示最近记录。
- 修正手动存档和自动存档的回合文案偏移。
- 大厅结束态改为明确的 `ENDED ROOM`，提供“查看最近记录”入口。

## 门禁状态

- Go 全量测试、`go vet ./...`：通过（使用临时 `GOCACHE`）。
- Go MySQL 8.4 targeted integration：通过（Friend/Chat/Group/Room 服务 34 个测试）。
- Vue 全量测试与生产构建：通过。
- OpenAPI lint、迁移测试、Compose 配置校验：通过。
- Python 项目 `.venv` 测试：109 个通过。
- 提交 `500b8ab` 的 [GitHub Actions CI #54](https://github.com/Chalice-faith/trpggame/actions/runs/35846287294)：七个作业全部成功；Go job 的 Linux targeted race 通过，双实例 Redis 总线自动化场景随 Go 测试通过。

结论：M2.5-A—F 的代码、自动化、远程 CI 和确定性 AI fixture 边界内的三账号真实浏览器验收通过；生产 DeepSeek、Milvus、真实域名/Nginx/TLS 和目标基础设施仍按 [主分支合并与真实验收清单](./M2.5主分支合并与真实验收清单.md) 验收，不提前标记为生产验收通过。
