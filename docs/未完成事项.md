# TRPGGame 未完成事项

> **更新日期**：2026-09-08
> **当前阶段**：Phase 1 / M1.6 暂缓；Phase 2 规划中
> **当前分支**：`dev/feature`  
> **清单建立基线**：`62b3063 feat: 接入 Swagger 接口文档`

## 1. 文档用途

本文档是项目未完成工作的唯一汇总清单。后续每完成、阻塞或新增一项工作，都应同步更新其状态、验证证据和关联提交。

状态统一使用：`未开始`、`进行中`、`等待外部条件`、`已完成`、`已取消`。

当前已经完成单人游戏后端、WebSocket 行动流式链路、Vue 单人游戏界面、公共 Go REST OpenAPI 规范和 Swagger UI。GitHub Actions 已通过，但尚未在真实部署环境完成接口与端到端验收，因此 Phase 1 仍不能标记为完成。

## 2. Phase 1 必须完成的事项

当前剩余 **4 项**。`P1-01` 已完成并通过 GitHub Actions；`P1-02` 和 `P1-03` 依赖开发者完成部署与实测，`P1-04` 是否产生代码修改取决于测试结果。

> **阶段切换记录（2026-09-08）**：开发者决定暂时跳过 Phase 1 部署和接口验收，转入 Phase 2 规划设计。P1-02 至 P1-05 保持未关闭，后续恢复时继续执行。

| 编号 | 事项 | 状态 | 责任方 | 前置条件 | 完成标准 |
|---|---|---|---|---|---|
| P1-01 | OpenAPI 契约自动校验并接入 CI | 已完成 | Codex | 用户已确认 | 自动比对 Gin 公共 REST 路由与 OpenAPI；Redocly 规范校验已接入 CI；本地验证及 GitHub Actions 均通过（2026-09-08） |
| P1-02 | 部署完整服务及真实依赖 | 等待外部条件 | 开发者 | 准备环境变量、DeepSeek 密钥及 Docker 环境 | MySQL、Redis、MinIO、Milvus、Python AI、Go、Vue、Nginx 均正常启动，日志无阻塞错误 |
| P1-03 | 执行真实接口与端到端测试 | 等待外部条件 | 开发者 | P1-02 完成 | 按《API接口测试文档》完成 REST、WebSocket 和 AI 链路测试，并记录响应、日志和失败项 |
| P1-04 | 修复联调发现的问题 | 等待外部条件 | Codex | 收到 P1-03 的测试结果 | 所有阻塞 Phase 1 的缺陷修复并补充回归测试；若未发现缺陷，则记录为无需修改 |
| P1-05 | 回归验证并完成 Phase 1 验收记录 | 等待外部条件 | Codex + 开发者 | P1-01 至 P1-04 完成 | 自动化检查与真实全链路回归通过，性能指标完成测量，验收记录更新并由开发者确认 |

### P1-03 必测范围

- 注册、登录、刷新 Token、查询及修改个人信息。
- 上传正常 PDF，等待解析、切片、向量化完成，并验证详情和列表。
- 对失败剧本执行重新解析；删除剧本后确认对象、向量和数据库记录已清理。
- 快速开始单人游戏，验证真实 DeepSeek 开场叙事和 RAG 检索结果。
- 通过 WebSocket 连续执行多轮行动，验证流式叙事、骰子反馈和角色状态更新。
- 验证行动幂等、错误回合号、暂停、恢复、手动存档、自动存档、读档和结束游戏。
- 验证 WebSocket 断线重连、`sync` 补推、非法 Token 和越权房间访问。
- 记录 AI 首 Token 时间和 WebSocket 消息延迟，核对 Phase 1 性能目标。

## 3. 当前暂停边界

在 P1-02、P1-03 完成并提供结果前：

- 不启动架构重构。
- 不开始 Phase 2 功能开发。
- 不把 Docker Compose、真实依赖联调或 Phase 1 验收标记为通过。
- 仅处理文档、测试结果分析等不依赖部署的工作；新增代码开发需由开发者明确确认。

## 4. 不阻塞 Phase 1 的后续事项

以下工作不计入上述 5 项，不应在本阶段临时扩大范围：

| 事项 | 计划阶段 | 说明 |
|---|---|---|
| 剧本解析进度 WebSocket 推送 | 增强项 | 当前使用剧本详情轮询，不阻塞主流程 |
| 生产容器与三端 lint 配置完善 | 工程优化 | 在部署结果稳定后单独安排 |
| 前端依赖安全升级与主包拆分 | 工程优化 | 详见《开发问题记录》DEV-001 至 DEV-003 |
| Python 资源警告与依赖弃用处理 | 工程优化 | 详见《开发问题记录》DEV-004、DEV-005 |
| 关键事件表和语义记忆 | Phase 3 | `trigger_event` 当前未持久化 |
| 好友、IM、群组及多人房间 | Phase 2 | Phase 1 验收后再开始 |

## 5. 更新规则

1. 开始任务时将状态改为 `进行中`，并写明实际负责人。
2. 外部环境、凭据或测试结果未就绪时使用 `等待外部条件`，不得标记完成。
3. 完成任务时补充验证命令、测试结果、关联提交或验收记录链接。
4. 联调发现的新问题先写入《开发问题记录》，确认阻塞 Phase 1 后再追加到本清单。
5. Phase 1 只有在 P1-01 至 P1-05 全部完成并经开发者确认后才能关闭。

## 6. 相关文档

- [API 接口测试文档](./API接口测试文档.md)
- [部署指南](./部署指南.md)
- [开发暂停交接](./开发暂停交接.md)
- [开发问题记录](./开发问题记录.md)
- [M1.3 验收记录](./M1.3验收记录.md)
